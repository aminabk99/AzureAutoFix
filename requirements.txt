fastapi==0.115.0
uvicorn[standard]==0.29.0
torch
httpx==0.27.0
pydantic==2.10.6
python-dotenv==1.0.1
msal==1.28.0
# Streamlit removed -- the UI is now frontend/index.html, a static page served
# by FastAPI at /app. Sign-in is handled browser-side by MSAL.js (CDN), so the
# `msal` package above is only used for the server-side client-credentials
# flow in backend/auth.py.
